Your RDS details
Endpoint
referraldb-instance.cilgusqwe0je.us-east-1.rds.amazonaws.com
Database name
referraldb

Username
👉 (you chose this when creating RDS — usually postgres)
Password
👉 the one you set in RDS (NOT from your old .env)

/referral/dev/db/url  
referraldb-instance.cilgusqwe0je.us-east-1.rds.amazonaws.com

DATABASE_URL=postgresql://postgres:postgres@postgres:5432/referral_db

postgresql://postgres:<YOUR_RDS_PASSWORD>@referraldb-instance.cilgusqwe0je.us-east-1.rds.amazonaws.com:5432/referraldb

modified
postgresql://postgres:test1234@referraldb-instance.cilgusqwe0je.us-east-1.rds.amazonaws.com:5432/referraldb

Store in Parameter Store:
Name: /ecommerce/dev/db/url
Type: SecureString
Value: postgresql://postgres:<YOUR_RDS_PASSWORD>@referraldb-instance.cilgusqwe0je.us-east-1.rds.amazonaws.com:5432/referraldb

✅ 3️⃣ Attach to ECS
In Task Definition:
ENV Name	Value
DATABASE_URL	/ecommerce/dev/db/url

4️⃣ VERY IMPORTANT CHECK (don’t skip)
🔐 Security Group

Go to RDS → Security Group:
Add rule:
Type: PostgreSQL
Port: 5432
Source: ECS Security Group

👉 Without this → ❌ Prisma will fail to connect

/ecommerce/dev/jwt/secret
/ecommerce/dev/email/password
/ecommerce/dev/mailgun/api_key

In Parameter Store:
DATABASE_URL
JWT_SECRET
In ECS env variables:
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90
-----------------------------------------------------------
🧠 Two valid ways to handle DB config
✅ Approach 1 (what YOU are using) — Full URL
DATABASE_URL=postgresql://user:password@host:5432/dbname

👉 Everything in one variable

✔ Pros:
Simple (best for beginners)
Works directly with Prisma
Less setup in ECS
✅ Approach 2 (tutorial style) — Split variables
DB_HOST=...
DB_USER=...
DB_PASSWORD=...
DB_NAME=...

Then in code:

const DATABASE_URL = `postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:5432/${DB_NAME}`;
🤔 Why tutorials use split variables?

👉 Because it’s more flexible & secure in large systems

Benefits:
Rotate password without touching full URL
Share host across services
Easier secrets management

You are using:

DATABASE_URL=postgresql://user:password@host:5432/db

👉 That already includes:

host ✅
password ✅
db name ✅

Your setup (clean & correct)
Parameter Store:
✅ DATABASE_URL
✅ JWT_SECRET
ECS:
inject env vars
Backend:
uses process.env.DATABASE_URL
-----------------------------------------------
you must create Target Groups in side EC2 not In VPC under private Link 

otherwise while you crate ALB you don't get TG

---------------------------------------------------
To find your ECR registry URL:
Console: ECR Console → Repositories → Click any repository → Copy the URI (everything before the repository name)

<account-id>.dkr.ecr.<your-region>.amazonaws.com
124045327524.dkr.ecr.us-east-1.amazonaws.com/referral/backend-service

Build and Push Product Service Image

1. Get ECR login command:
aws ecr get-login-password --region <your-region> | docker login --username AWS --password-stdin <account-id>.dkr.ecr.<your-region>.amazonaws.com

modified:
aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 124045327524.dkr.ecr.us-east-1.amazonaws.com

2. Build the image:
cd referral-hub-backend
docker build -t referral-hub-backend .

3. Tag the image:
docker tag referral-hub-backend:latest 124045327524.dkr.ecr.us-east-1.amazonaws.com/referral/backend-service:latest

4. Push the image:
docker push 124045327524.dkr.ecr.us-east-1.amazonaws.com/referral/backend-service:latest

--------------------------------------------------
ECS Task 

name: ecsTaskExecutionRole 
police: AmazonECSTaskExecutionRolePolicy

The AmazonECSTaskExecutionRolePolicy is an AWS-managed IAM policy attached to your ECS task execution role. 
It authorizes the ECS and Fargate infrastructure agents to perform backend operational tasks on your behalf.

Specifically, it is used to:
Pull images: Download container images from Amazon Elastic Container Registry (ECR).
Write logs: Send container logs to Amazon CloudWatch.
Inject secrets: Retrieve sensitive data from AWS Secrets Manager or Systems Manager Parameter Store.


------------------------------------------------------
The AWS SDK (Software Development Kit) is a collection of libraries and tools that lets developers connect their application code directly to AWS services.

-----------------------------------------------------
An ECS Cluster is a logical grouping of compute resources (servers) where your containers run, while an ECS 
Service ensures a specific number of those containers are always active and healthy.
-----------------------------------------------------
RDS Security Group
name: referral-rds-sg

Inbound rules 
type                source
  PostgreSQL            custom: referral-ecs-sg
  PostgreSQL                    10.10.0.0/16

----------------------------------------------------
Bastion
Test API Endpoints:

jemal@DESKTOP-BL4RJLS:/mnt/c/USers/Dell/Downloads$
1. Move the key into WSL home directory
cp referral-ec2-key.pem ~/
cd ~

2. Set correct permissions again
chmod 400 referral-ec2-key.pem

3. Connect again
SSH into bastion host:
ssh -i your-key.pem ec2-user@<bastion-public-ip>
ssh -i referral-ec2-key.pem ec2-user@3.82.195.72

4. Test product service:
curl http://<internal-alb-dns-name>/products
curl http://internal-referral-internal-alb-905708149.us-east-1.elb.amazonaws.com/health
curl http://internal-referral-internal-alb-905708149.us-east-1.elb.amazonaws.com/api/v1/users

[ec2-user@ip-10-10-1-131 ~]$ curl -X POST http://internal-referral-internal-alb-905708149.us-east-1.elb.amazonaws.com/ap
i/v1/users/signup \
> -H "content-type: application/json" \
> -d '{ "name" : "Admin", "email": "admin@gmail.com","phone": "0923433343", "password": "test1234", "passwordConfirm": "test1234", "address": "addis ababa","role": "admin", "status": "active"}'

login
[ec2-user@ip-10-10-1-131 ~]$ curl -X POST http://internal-referral-internal-alb-905708149.us-east-1.elb.amazonaws.com/api/v1/users/login \
> -H "content-type: application/json" \
> -d '{ "email": "admin@gmail.com", "password": "test1234" }'

note:
Location	Behavior
/mnt/c/...	❌ Windows filesystem (breaks chmod)
~/ (Linux home)	✅ Proper Linux permissions

------------------------------------------------------------
✅ REAL FIX (what you should do)
🚀 OPTION 1 (BEST) — Generate migrations on host

👉 Exit container:

exit

👉 Fix .env:

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/referral_db

👉 Run on your machine:

npx prisma migrate dev --name init
✅ Result

Now migrations will be saved in:

referral-hub-backend/prisma/migrations/ ✅
------------------------------------------------------------
✅ Better approach (recommended)

Instead of always latest, use version tags:

backend-service:v1
backend-service:v2
backend-service:v3

Example:

docker tag referral-hub-backend:latest 124045327524.dkr.ecr.us-east-1.amazonaws.com/referral/backend-service:v2
docker push 124045327524.dkr.ecr.us-east-1.amazonaws.com/referral/backend-service:v2

Then update ECS task definition to v2

🧠 Best Practice Summary
Thing	Recommendation
Delete ECR	❌ Never
Use same repo	✅ Yes
Use latest tag	⚠️ Only for testing
Use version tags	✅ Best practice
Rebuild & push	✅ Every change

IMAGE                                                                          ID             DISK USAGE   CONTENT SIZE   EXTRA
124045327524.dkr.ecr.us-east-1.amazonaws.com/referral/backend-service:latest   a21ee01c54fd       2.12GB          561MB
124045327524.dkr.ecr.us-east-1.amazonaws.com/referral/backend-service:v2

In Amazon Elastic Container Service (ECS):

👉 You DO NOT update containers directly

👉 You update:

✅ Task Definition

🧠 Key concept

ECS flow:

ECR image → Task Definition → Service → Running Containers

So if you want to use v2, you must:

👉 create new task definition revision

✅ Step-by-step (THIS is what you need)
🟢 1. Go to ECS Console
Open Cluster
Click your Service
🟢 2. Click “Task Definition”

You’ll see something like:

backend-service:1
🟢 3. Create new revision

Click:

👉 Create new revision

🟢 4. Update image

Find container section and change:

- referral/backend-service:latest
+ referral/backend-service:v2

Full example:

124045327524.dkr.ecr.us-east-1.amazonaws.com/referral/backend-service:v2
🟢 5. Save

👉 This creates:

backend-service:2
🟢 6. Update Service

Now go back to Service:

👉 Click Update Service

Then:

Select new task definition revision (:2)
Click Deploy
🔄 What happens next

ECS will:

Stop old containers
Pull new image (v2)
Start new containers
🚨 If you don’t see v2

Check:

1. Did you push it?
docker push <repo>:v2
2. Confirm in ECR

Go to repo → check tag list

--------------------------------------------------
🧪 STEP 4 — Verify data in RDS (optional)

From bastion:

LOGIN
[ec2-user@ip-10-10-1-131 ~]$ curl -X POST http://internal-referral-internal-alb-905708149.us-east-1.elb.amazonaws.com/api/v1/users/login \
> -H "content-type: application/json" \
> -d '{ "email": "admin@gmail.com", "password": "test1234" }'

✅ Fix: Install psql on EC2

Since you’re on Amazon Linux (EC2), run:

sudo yum install postgresql15 -y

RDS endpoint : referraldb-instance.cilgusqwe0je.us-east-1.rds.amazonaws.com
username : referraldb

psql -h referraldb-instance.cilgusqwe0je.us-east-1.rds.amazonaws.com \
-U postgres \
-d referraldb

🔐 It will ask for password

Enter your RDS password (you won’t see it while typing — normal)

✅ If everything is correct, you’ll see:
referraldb=>

SELECT * FROM "User";

🚀 Useful commands (you’ll need these)
Show tables
\dt

Describe users table
\d "User"

Exit psql
\q




Fix:
ECS SG allows ALB ✅
Bastion SG must allow outbound ✅
ALB must allow inbound from bastion SG (if restricted)
❌ Endpoint wrong

Try:

/health
/
/api/v1
❌ No response

Check:

ECS tasks running
Target group healthy
🚀 BONUS (Postman way)

Instead of curl:

Create request
URL:http://<internal-alb-dns-name>/api/v1/users/signup
Body → JSON
Send

--------------------------------------------------------------------
The primary purpose of an AWS VPC link is to securely connect Amazon API Gateway APIs to private backend resources inside 
a Virtual Private Cloud (VPC) without exposing those resources to the public internet

------------------------------------------------------------------
apiGateway

Routes Let Express handle routing logic (best design)
ANY /{proxy+}
OPTIONS /{proxy+}

❌ This will NOT work:
/api/v1/users/:userId/patients

Express         	API Gateway
:userId   	      {userId}
nested routes	    use {proxy+}

--------------------------------------------------------------------
Request from browser:
Origin: https://your-frontend-domain.com

👉 Backend must allow THIS origin ✅

🚀 If you're using S3 + CloudFront

Not use API Gateway URL in CORS origin, Your frontend URL will look like:

https://d123xyz.cloudfront.net

👉 Then update CORS:

app.use(cors({
  origin: [
    'https://d123xyz.cloudfront.net'
  ],
  credentials: true
}))

✅ Final Setup Summary
Backend (Express)
app.use(cors({
  origin: [
    'http://localhost:3000',
    'https://your-frontend-domain.com'
  ],
  credentials: true
}))

Frontend (Next.js) (apiGateway Invoke Url)
NEXT_PUBLIC_CLIENT_API_URL=https://abc123.execute-api.us-east-1.amazonaws.com

API Gateway
No CORS needed here (since backend handles it)
Use:
ANY /{proxy+}

----------------------------------------------------------------------
User → S3 Website (React)
     → API Gateway
     → VPC Link
     → ALB
     → ECS Backend

⚠️ First Important Thing (Next.js)

S3 can only host static websites, so:
👉 You MUST export your Next.js app as static

✅ Step 1: Configure Next.js for static export
In your project, update:

next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',   // 🔥 important
};
module.exports = nextConfig;

✅ Step 2: Build your frontend
Run:
npm run build
👉 This creates:
/out

This folder contains:
index.html
static JS/CSS

BUT 

❗ Root Problem while to upload s3 tring to  

You got this error:

Page "/[...not-found]" is missing "generateStaticParams()" so it cannot be used with "output: export"

👉 This means:

You are using App Router (Next.js 13/14/15)
AND using dynamic routes
BUT output: 'export' requires fully static pages

🔥 BEST SOLUTION (recommended for you)
Since your app uses:
API calls
auth (JWT)
dynamic users

👉 Use this instead:
  REMOVE this:
  output: 'export'

Then deploy differently (better for your case)
Instead of S3 static:
👉 Use:
Vercel ✅ (easiest)
OR EC2 + Nginx
OR Docker (same as backend)

⚠️ Important Reality Check
👉 Your app is NOT fully static because:
uses API (axios)
uses auth token
dynamic routes

🚀 What I recommend (honest advice)
👉 DO THIS:
Option 1 (BEST)
Deploy frontend on Vercel
Keep backend on AWS

Option 2
Use Docker for frontend
Deploy to ECS (same as backend)

Option 3 (only static sites)
Use S3 (but requires no dynamic routes)
👉 So S3-only hosting is NOT ideal


EC2 + Nginx is perfect for your app (since it’s dynamic and uses API + auth).
🧭 Architecture
User → Nginx (EC2)
     → Next.js frontend (port 3000)
     → API Gateway
     → Backend (ECS)
     

--------------------------------------------------------------------------------
setup

SSH: 
ssh -i referral-ec2-key.pem ec2-user@54.159.187.200




if REMOVE
 sudo dnf remove nodejs -y
 sudo dnf install nodejs -y
 node -v
 v20.x.x ✅

cd referral-frontend-nginx
rm -rf node_modules package-lock.json
npm install
npm run build

If you want FAST installs always: (only if package-lock.json exists)
Ctrl + C
npm cache clean --force
npm install --no-audit --no-fund

